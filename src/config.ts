type GameConfig = {
  playerHealth: number
  diceFaces: number
  numberOfDice: number
}

const gameConfig: GameConfig = {
  playerHealth: 20,
  diceFaces: 6,
  numberOfDice: 2,
}

export default gameConfig
